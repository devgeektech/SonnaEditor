"""Mode B rebuild track (HANDOVER Part 6 item 17).

Step 1 = style survey (this module's `survey.py`).
Step 2 = preset-to-checkpoint converter (not yet built).
Step 3 = Mode B inference path (reuses Mode A's inference engine).
"""
