"""Sonna Editor HTTP API — FastAPI bridge between the React/Electron UI and the ML pipeline.

7.1a: read-only endpoints (health, profiles, folder scan, captures).
7.1b will add async jobs, websocket streaming, and the /api/process and /api/finetune
implementations.
"""
